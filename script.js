const siteConfig = {
  conference: {
    name: "Queen's Global Energy Conference",
    year: "2026",
    tagline: "Canada's premier university energy conference",
    description:
      "Join students, researchers, executives, and policy leaders for a one-day conference focused on the global energy industry.",
    about:
      "Queen's Global Energy Conference (QGEC) connects emerging talent with decision-makers shaping global energy markets. The conference combines keynote perspectives, technical sessions, and practical networking designed for both learning and sponsorship visibility.",
    heroImage: "images/backroundsvgs/hero-energy-outline.svg",
    aboutImage: "images/western-electricity-grids.jpg"
  },
  ctaLinks: {
    registerUrl: "registration.html",
    sponsorUrl: "mailto:qgec@engsoc.queensu.ca",
    sponsorInfoUrl: "sponsorship-package.html",
    prospectusUrl: "sponsorship-package.html"
  },
  themes: [
    {
      title: "Energy Markets",
      description: "Trade flows, market volatility, and long-term demand outlooks.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    },
    {
      title: "Policy and Regulation",
      description: "Frameworks accelerating resilient and affordable energy systems.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    },
    {
      title: "Infrastructure and Grid",
      description: "Grid modernization, storage, and transmission priorities.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    },
    {
      title: "Climate and Transition",
      description: "Decarbonization strategy and realistic implementation pathways.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    },
    {
      title: "Technology and Innovation",
      description: "New technologies from advanced materials to clean fuels.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    },
    {
      title: "Careers in Energy",
      description: "Mentorship, recruiting, and pathways into energy leadership.",
      image: "images/backroundsvgs/theme-placeholder.svg"
    }
  ],
  speakers: [
    {
      name: "Jeff Bezos",
      title: "CEO",
      organization: "Amazon",
      image: "images/jeffbezos.jpg"
    },
    {
      name: "Mark Diederichs",
      title: "Founder",
      organization: "Tunnels",
      image: "images/mark.jpeg"
    },
    {
      name: "Elon Musk",
      title: "CEO",
      organization: "Boring Company",
      image: "images/elonmusk.jpeg"
    }
  ],
  organizingCommittee: {
    coChairs: [
      {
        name: "Alison Whike",
        role: "Co-Chair",
        image: "images/Team/Cochair/Aly-Headshot-opt.jpg"
      },
      {
        name: "Matias Marjovsky",
        role: "Co-Chair",
        image: "images/Team/Cochair/Matias-Headshot-opt.jpg"
      }
    ],
    directors: [
      {
        name: "Avery Phelan",
        role: "Logistics",
        image: "images/Team/Directors/Avery-Headshot-opt.jpg"
      },
      {
        name: "Gabriel Soler",
        role: "Finance",
        image: "images/Team/Directors/Gabe-Headshot-opt.jpg"
      },
      {
        name: "Jamie Greenberg",
        role: "Speakers",
        image: "images/Team/Directors/Jamie-Headshot-opt.jpg"
      },
      {
        name: "Markus Pennant",
        role: "Sponsorship",
        image: "images/Team/Directors/Markus-Headshot-opt.jpg"
      },
      {
        name: "Sarah Becanin",
        role: "X",
        image: "images/backroundsvgs/speaker-placeholder.svg"
      },
    ]
  },
  agenda: [
    {
      time: "09:00",
      title: "Registration and Coffee",
      speaker: "QGEC Organizing Committee",
      location: "Main Lobby"
    },
    {
      time: "10:00",
      title: "Opening Keynote",
      speaker: "TBD",
      location: "Main Hall"
    },
    {
      time: "11:15",
      title: "Panel: Global Energy Security",
      speaker: "TBD",
      location: "Main Hall"
    },
    {
      time: "12:30",
      title: "Networking Lunch",
      speaker: "-",
      location: "Atrium"
    },
    {
      time: "13:45",
      title: "Fireside: Investment in Energy",
      speaker: "TBD",
      location: "Main Hall"
    },
    {
      time: "15:00",
      title: "Student Innovation Showcase",
      speaker: "TBD",
      location: "Breakout Rooms"
    },
    {
      time: "16:15",
      title: "Closing Remarks",
      speaker: "TBD",
      location: "Main Hall"
    },
    {
      time: "17:00",
      title: "Reception",
      speaker: "-",
      location: "Atrium"
    }
  ],
  pastSponsors: [
    { name: "RBC Capital Markets", logo: "images/Sponsors/rbccm.png" },
    { name: "Cannat", logo: "images/Sponsors/cannat.png" },
    { name: "DDIC", logo: "images/Sponsors/DDIC.webp" },
    { name: "GWLG", logo: "images/Sponsors/GWLG.png" },
    { name: "OWA", logo: "images/Sponsors/OWA.png" },
    { name: "TTA", logo: "images/Sponsors/TTA.png" },
    { name: "YCR", logo: "images/Sponsors/YCR.webp" },
    { name: "Bennett Jones", logo: "images/Sponsors/bennettjones.jpg" },
    { name: "Bullfrog Power", logo: "images/Sponsors/bullfrog.svg" },
    { name: "CEEA", logo: "images/Sponsors/ceea.svg" },
    { name: "Hemisphere", logo: "images/Sponsors/hemisphere.png" },
    { name: "McDaniel", logo: "images/Sponsors/mcdaniel.webp" },
    { name: "Orennia", logo: "images/Sponsors/orennia.png" },
    { name: "Smith", logo: "images/Sponsors/smith.png" },
    { name: "WaterPower", logo: "images/Sponsors/waterpower.png" }
  ],
  venue: {
    name: "Goodes Hall",
    address: "143 Union Street",
    mapEmbedUrl: "TBD",
    notes: "Travel details, accommodation blocks, and parking guidance will be published here.",
    placeholderImage: "images/goodes-hall.jpg"
  },
  faq: [
    {
      question: "When does registration open?",
      answer: "Registration timeline is TBD. Updates will be posted on this page and official social channels."
    },
    {
      question: "Are students from all faculties welcome?",
      answer: "Yes. QGEC is open to students and professionals with interests in energy, policy, finance, technology, and sustainability."
    },
    {
      question: "How can organizations become sponsors?",
      answer: "Use the sponsorship contact button above. Our team will share prospectus tiers and partnership options."
    },
    {
      question: "Will sessions be recorded?",
      answer: "Recording availability is TBD and depends on speaker permissions and session format."
    }
  ],
  contact: {
    email: "qgec@engsoc.queensu.ca",
    address: "45 Union St, Kingston, Ontario",
    sponsorEmail: "qgec@engsoc.queensu.ca",
    social: [
      { label: "LinkedIn", url: "https://www.linkedin.com/company/qgec/" },
      { label: "Instagram", url: "https://www.instagram.com/queens_qgec/" }
    ]
  }
};

const FALLBACK = "TBD";
const DEFAULT_PLACEHOLDER = {
  theme: {
    title: "Theme Coming Soon",
    description: "Program details are being finalized.",
    image: "images/backroundsvgs/theme-placeholder.svg"
  },
  speaker: {
    name: "TBD Speaker",
    title: FALLBACK,
    organization: FALLBACK,
    image: "images/backroundsvgs/speaker-placeholder.svg"
  },
  coChair: {
    name: "TBD Co-Chair",
    role: "Conference Co-Chair",
    image: "images/backroundsvgs/speaker-placeholder.svg"
  },
  director: {
    name: "TBD Director",
    role: "Director",
    image: "images/backroundsvgs/speaker-placeholder.svg",
    coordinators: [
      { name: "TBD Coordinator", role: "Coordinator" },
      { name: "TBD Coordinator", role: "Coordinator" }
    ]
  },
  sponsor: {
    name: "Past Sponsor",
    logo: "images/backroundsvgs/sponsor-placeholder.svg",
    url: ""
  },
  agenda: {
    time: "09:00",
    title: "Schedule Coming Soon",
    speaker: "TBD",
    location: "TBD"
  }
};

const byId = (id) => document.getElementById(id);

const safeText = (value, fallback = FALLBACK) => {
  if (typeof value === "string" && value.trim()) {
    return value.trim();
  }
  return fallback;
};

const safeUrl = (url, fallback = "#") => {
  if (typeof url !== "string") {
    return fallback;
  }

  const trimmed = url.trim();
  if (!trimmed || trimmed.toUpperCase() === FALLBACK) {
    return fallback;
  }
  return trimmed;
};

const asArray = (value) => (Array.isArray(value) ? value : []);

const setLink = (element, url, fallback = "#") => {
  const href = safeUrl(url, fallback);
  element.href = href;

  if (/^https?:\/\//i.test(href)) {
    element.target = "_blank";
    element.rel = "noopener noreferrer";
    return;
  }

  element.removeAttribute("target");
  element.removeAttribute("rel");
};

const renderHero = () => {
  const { conference, ctaLinks } = siteConfig;
  byId("hero-title").textContent = `${safeText(conference?.name)} ${safeText(conference?.year, "")}`.trim();
  byId("hero-tagline").textContent = safeText(conference?.tagline);
  byId("hero-description").textContent = safeText(conference?.description);
  byId("hero-media").style.backgroundImage = `url('${safeUrl(conference?.heroImage, "images/backroundsvgs/hero-energy-outline.svg")}')`;

  const register = safeUrl(ctaLinks?.registerUrl);
  const sponsor = safeUrl(ctaLinks?.sponsorInfoUrl, safeUrl(ctaLinks?.sponsorUrl));

  ["hero-register-link", "header-register-link", "header-register-cta"].forEach((id) => {
    const link = byId(id);
    if (link) {
      setLink(link, register);
    }
  });

  setLink(byId("hero-sponsor-link"), sponsor);
};

const renderAbout = () => {
  const { conference } = siteConfig;
  byId("about-description").textContent = safeText(conference?.about);

  const image = byId("about-image");
  image.src = safeUrl(conference?.aboutImage, "images/backroundsvgs/about-placeholder.svg");
  image.alt = `${safeText(conference?.name)} audience placeholder image`;
};

const renderThemes = () => {
  const container = byId("themes-grid");
  if (!container) {
    return;
  }
  container.innerHTML = "";
  const themes = asArray(siteConfig.themes);

  (themes.length ? themes : [DEFAULT_PLACEHOLDER.theme, DEFAULT_PLACEHOLDER.theme, DEFAULT_PLACEHOLDER.theme]).forEach((theme) => {
    const card = document.createElement("article");
    card.className = "theme-card";
    card.innerHTML = `
      <img src="${safeUrl(theme?.image, "images/backroundsvgs/theme-placeholder.svg")}" alt="${safeText(theme?.title)} theme image" loading="lazy" />
      <div class="theme-card-content">
        <h3>${safeText(theme?.title)}</h3>
        <p>${safeText(theme?.description)}</p>
      </div>
    `;
    container.append(card);
  });
};

const renderSpeakers = () => {
  const container = byId("speakers-grid");
  if (!container) {
    return;
  }
  container.innerHTML = "";
  const speakers = asArray(siteConfig.speakers);

  (speakers.length ? speakers : [DEFAULT_PLACEHOLDER.speaker, DEFAULT_PLACEHOLDER.speaker, DEFAULT_PLACEHOLDER.speaker]).forEach((speaker) => {
    const card = document.createElement("article");
    card.className = "speaker-card";
    card.innerHTML = `
      <img src="${safeUrl(speaker?.image, "images/backroundsvgs/speaker-placeholder.svg")}" alt="${safeText(speaker?.name)} headshot" loading="lazy" />
      <div class="speaker-copy">
        <h3 class="speaker-name">${safeText(speaker?.name)}</h3>
        <p class="speaker-meta">${safeText(speaker?.title)} | ${safeText(speaker?.organization)}</p>
      </div>
    `;
    container.append(card);
  });
};

const renderTeam = () => {
  const committee = siteConfig.organizingCommittee || {};

  const coChairsContainer = byId("cochairs-grid");
  const directorsContainer = byId("directors-grid");
  coChairsContainer.innerHTML = "";
  directorsContainer.innerHTML = "";

  const coChairs = asArray(committee.coChairs).slice(0, 2);

  coChairs.forEach((member) => {
    const card = document.createElement("article");
    card.className = "cochair-person team-person";
    card.innerHTML = `
      <img
        class="team-photo team-photo-cochair"
        src="${safeUrl(member?.image, "images/backroundsvgs/speaker-placeholder.svg")}"
        alt="${safeText(member?.name)} photo"
        decoding="async"
        loading="lazy"
      />
      <h3 class="team-name">${safeText(member?.name)}</h3>
      <p class="team-role">${safeText(member?.role)}</p>
    `;
    coChairsContainer.append(card);
  });

  const directors = asArray(committee.directors).slice(0, 5);

  directors.forEach((director) => {
    const card = document.createElement("article");
    card.className = "director-person team-person";

    card.innerHTML = `
      <img
        class="team-photo team-photo-director"
        src="${safeUrl(director?.image, "images/backroundsvgs/speaker-placeholder.svg")}"
        alt="${safeText(director?.name)} photo"
        decoding="async"
        loading="lazy"
      />
      <h3 class="team-name">${safeText(director?.name)}</h3>
      <p class="team-role">${safeText(director?.role)}</p>
    `;

    directorsContainer.append(card);
  });
};

const parseTimeToMinutes = (timeLabel) => {
  const time = safeText(timeLabel, "").toUpperCase();
  if (!time) {
    return Number.MAX_SAFE_INTEGER;
  }

  const hhmm24 = time.match(/^(\d{1,2}):(\d{2})$/);
  if (hhmm24) {
    return Number(hhmm24[1]) * 60 + Number(hhmm24[2]);
  }

  const ampm = time.match(/^(\d{1,2})(?::(\d{2}))?\s*(AM|PM)$/);
  if (ampm) {
    let hours = Number(ampm[1]) % 12;
    const minutes = Number(ampm[2] || "0");
    if (ampm[3] === "PM") {
      hours += 12;
    }
    return hours * 60 + minutes;
  }

  return Number.MAX_SAFE_INTEGER;
};

const renderAgenda = () => {
  const agendaList = byId("agenda-list");
  if (!agendaList) {
    return;
  }
  agendaList.innerHTML = "";

  const defaultAgenda = [
    { time: "09:00", title: "Registration and Coffee", speaker: "TBD", location: "TBD" },
    { time: "10:00", title: "Opening Keynote", speaker: "TBD", location: "TBD" },
    { time: "11:30", title: "Panel Discussion", speaker: "TBD", location: "TBD" },
    { time: "13:00", title: "Networking Lunch", speaker: "-", location: "TBD" },
    { time: "14:00", title: "Breakout Sessions", speaker: "TBD", location: "TBD" },
    { time: "15:30", title: "Fireside Chat", speaker: "TBD", location: "TBD" },
    { time: "17:00", title: "Closing", speaker: "TBD", location: "TBD" }
  ];

  const agendaItems = (asArray(siteConfig.agenda).length ? asArray(siteConfig.agenda) : defaultAgenda)
    .slice()
    .sort((a, b) => parseTimeToMinutes(a?.time) - parseTimeToMinutes(b?.time));

  (agendaItems.length ? agendaItems : [DEFAULT_PLACEHOLDER.agenda]).forEach((entry) => {
    const li = document.createElement("li");
    li.className = "agenda-item";
    li.innerHTML = `
      <p class="agenda-time">${safeText(entry?.time)}</p>
      <div>
        <h3 class="agenda-session">${safeText(entry?.title)}</h3>
        <p class="agenda-details">Speaker: ${safeText(entry?.speaker)} | Location: ${safeText(entry?.location)}</p>
      </div>
    `;
    agendaList.append(li);
  });
};

const renderSponsors = () => {
  const banner = byId("past-sponsors-banner");
  banner.innerHTML = "";

  const sponsors = asArray(siteConfig.pastSponsors).slice();
  const fallbackSponsors = Array.from({ length: 10 }, (_, index) => ({
    ...DEFAULT_PLACEHOLDER.sponsor,
    name: `Past Sponsor ${index + 1}`
  }));

  const createSponsorLogoNode = (sponsor) => {
    const href = safeUrl(sponsor?.url, "");
    const logoSrc = safeUrl(sponsor?.logo, "images/backroundsvgs/sponsor-placeholder.svg");
    const card = href ? document.createElement("a") : document.createElement("article");
    card.className = "sponsor-banner-item";
    card.setAttribute("aria-label", safeText(sponsor?.name));

    if (href) {
      setLink(card, href);
    }

    card.innerHTML = `
      <img src="${logoSrc}" alt="${safeText(sponsor?.name)} logo" loading="lazy" />
    `;
    return card;
  };

  const logoItems = sponsors.length ? sponsors : fallbackSponsors;
  logoItems.forEach((sponsor) => {
    banner.append(createSponsorLogoNode(sponsor));
  });

  setLink(byId("prospectus-link"), siteConfig.ctaLinks?.prospectusUrl);

  const sponsorEmail = safeText(siteConfig.contact?.sponsorEmail, "");
  const sponsorFallback = sponsorEmail ? `mailto:${sponsorEmail}` : "#";
  setLink(byId("sponsor-link"), siteConfig.ctaLinks?.sponsorUrl, sponsorFallback);
};

const renderVenue = () => {
  const venue = siteConfig.venue || {};
  const venueName = byId("venue-name");
  const venueAddress = byId("venue-address");
  const venueNotes = byId("venue-notes");
  const mapContainer = byId("venue-map");

  if (!venueName || !venueAddress || !venueNotes || !mapContainer) {
    return;
  }

  venueName.textContent = safeText(venue.name);
  venueAddress.textContent = safeText(venue.address);
  venueNotes.textContent = safeText(venue.notes);

  mapContainer.innerHTML = "";

  const mapUrl = safeUrl(venue.mapEmbedUrl, "TBD");
  if (mapUrl !== "TBD" && mapUrl !== "#") {
    const iframe = document.createElement("iframe");
    iframe.src = mapUrl;
    iframe.loading = "lazy";
    iframe.referrerPolicy = "no-referrer-when-downgrade";
    iframe.title = `${safeText(venue.name)} map`;
    mapContainer.append(iframe);
    return;
  }

  const placeholder = document.createElement("img");
  placeholder.src = safeUrl(venue.placeholderImage, "images/backroundsvgs/venue-map-placeholder.svg");
  placeholder.alt = "Venue map placeholder";
  placeholder.loading = "lazy";
  mapContainer.append(placeholder);
};

const setupFaq = () => {
  const container = byId("faq-list");
  container.innerHTML = "";

  asArray(siteConfig.faq).forEach((entry, index) => {
    const item = document.createElement("article");
    item.className = "faq-item";

    const questionId = `faq-question-${index}`;
    const panelId = `faq-panel-${index}`;

    item.innerHTML = `
      <button id="${questionId}" class="faq-question" type="button" aria-expanded="false" aria-controls="${panelId}">
        ${safeText(entry?.question)}
      </button>
      <p id="${panelId}" class="faq-answer" role="region" aria-labelledby="${questionId}" hidden>
        ${safeText(entry?.answer)}
      </p>
    `;

    container.append(item);
  });

  const buttons = [...container.querySelectorAll(".faq-question")];

  buttons.forEach((button, index) => {
    button.addEventListener("click", () => {
      const expanded = button.getAttribute("aria-expanded") === "true";
      button.setAttribute("aria-expanded", String(!expanded));
      const panel = byId(button.getAttribute("aria-controls"));
      panel.hidden = expanded;
    });

    button.addEventListener("keydown", (event) => {
      if (event.key === "ArrowDown") {
        event.preventDefault();
        buttons[(index + 1) % buttons.length].focus();
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        buttons[(index - 1 + buttons.length) % buttons.length].focus();
      }
      if (event.key === "Home") {
        event.preventDefault();
        buttons[0].focus();
      }
      if (event.key === "End") {
        event.preventDefault();
        buttons[buttons.length - 1].focus();
      }
    });
  });
};

const renderFooter = () => {
  const contact = siteConfig.contact || {};
  byId("contact-email").textContent = `Email: ${safeText(contact.email)}`;
  byId("contact-address").textContent = `Address: ${safeText(contact.address)}`;

  const socialLinks = byId("social-links");
  socialLinks.innerHTML = "";
  const social = asArray(contact.social);

  (social.length ? social : [{ label: "Conference Updates", url: "#" }]).forEach((item) => {
    const link = document.createElement("a");
    setLink(link, item?.url);
    link.textContent = safeText(item?.label);
    socialLinks.append(link);
  });

  byId("footer-copyright").textContent =
    `Copyright ${new Date().getFullYear()} ${safeText(siteConfig.conference?.name)}. All rights reserved.`;
};

const setupHeader = () => {
  const header = byId("top");
  const toggle = byId("menu-toggle");
  const nav = byId("site-nav");

  const closeNav = () => {
    header.classList.remove("nav-open");
    toggle.setAttribute("aria-expanded", "false");
    document.body.style.overflow = "";
  };

  toggle.addEventListener("click", () => {
    const expanded = toggle.getAttribute("aria-expanded") === "true";
    toggle.setAttribute("aria-expanded", String(!expanded));
    header.classList.toggle("nav-open", !expanded);
    document.body.style.overflow = expanded ? "" : "hidden";
  });

  [...nav.querySelectorAll("a")].forEach((link) => {
    link.addEventListener("click", closeNav);
  });

  document.addEventListener("click", (event) => {
    if (!header.contains(event.target)) {
      closeNav();
    }
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeNav();
    }
  });

  window.addEventListener("resize", () => {
    if (window.innerWidth > 1024) {
      closeNav();
    }
  });

};

const setupRevealAnimation = () => {
  const observed = [...document.querySelectorAll(".observe")];
  if (!("IntersectionObserver" in window)) {
    observed.forEach((node) => node.classList.add("is-visible"));
    return;
  }

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.14 }
  );

  observed.forEach((node) => observer.observe(node));
};

const init = () => {
  renderHero();
  renderAbout();
  renderThemes();
  renderSpeakers();
  renderTeam();
  renderAgenda();
  renderSponsors();
  renderVenue();
  setupFaq();
  renderFooter();
  setupHeader();
  setupRevealAnimation();
};

window.addEventListener("DOMContentLoaded", init);
